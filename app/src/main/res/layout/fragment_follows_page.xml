<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
     xmlns:android="http://schemas.android.com/apk/res/android"
     android:layout_height="match_parent"
     android:layout_width="match_parent"
     android:background="@drawable/bg_top_rounded_section_card">

    <androidx.recyclerview.widget.RecyclerView
         android:layout_height="match_parent"
         android:layout_width="match_parent"
         android:paddingBottom="100dp"
         android:clipToPadding="false"
         android:id="@+id/page_follows_rv"
         android:paddingTop="8dp" />

    <LinearLayout
         android:layout_height="match_parent"
         android:layout_width="match_parent"
         android:visibility="gone"
         android:gravity="center"
         android:orientation="vertical"
         android:paddingBottom="80dp"
         android:id="@+id/page_follows_empty">

        <ImageView
             android:layout_height="160dp"
             android:layout_width="160dp"
             android:src="@drawable/img_empty_library"
             android:contentDescription="Empty list"
             android:alpha="0.6" />

        <TextView
             android:layout_height="wrap_content"
             android:layout_width="wrap_content"
             android:gravity="center"
             android:textSize="19sp"
             android:fontFamily="@font/inter_semibold"
             android:textColor="?attr/colorOnSurface"
             android:layout_marginTop="20dp"
             android:id="@+id/page_follows_empty_title" />

        <TextView
             android:layout_height="wrap_content"
             android:layout_width="260dp"
             android:gravity="center"
             android:textSize="15sp"
             android:fontFamily="@font/inter_regular"
             android:textColor="?attr/colorOnSurfaceVariant"
             android:layout_marginTop="8dp"
             android:lineSpacingMultiplier="1.4"
             android:id="@+id/page_follows_empty_subtitle" />

    </LinearLayout>


    <!-- Loading indicator — shown while data is being fetched -->
    <ProgressBar
         android:id="@+id/page_follows_loading"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:layout_gravity="center"
         android:visibility="gone" />

</FrameLayout>