<?xml version="1.0" encoding="utf-8"?>
<!--
  Author's "My Works" novel card — Bluesky style
  • No raw elevation — hairline outline card
  • Rounded cover clipped by inner MaterialCardView
  • All text colors use ?attr/ tokens — dark mode safe
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/colorOutline"
    app:cardBackgroundColor="?attr/colorSurface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="14dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Rounded cover -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_cover_container"
            android:layout_width="80dp"
            android:layout_height="110dp"
            android:layout_marginEnd="14dp"
            app:cardCornerRadius="10dp"
            app:cardElevation="0dp"
            app:strokeWidth="0dp">

            <ImageView
                android:id="@+id/card_cover"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:background="@color/shimmer_base"
                android:contentDescription="Novel cover" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Text block -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_weight="1">

            <TextView
                android:id="@+id/card_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:textSize="16sp"
                android:fontFamily="@font/inter_semibold"
                android:textColor="?attr/colorOnSurface"
                android:maxLines="2" />

            <TextView
                android:id="@+id/card_meta"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="13sp"
                android:fontFamily="@font/inter_regular"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:layout_marginTop="5dp" />

            <TextView
                android:id="@+id/card_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingVertical="4dp"
                android:paddingHorizontal="10dp"
                android:textSize="12sp"
                android:fontFamily="@font/inter_semibold"
                android:layout_marginTop="8dp" />

        </LinearLayout>

        <ImageView
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src="@drawable/ic_arrow_right"
            android:contentDescription="Open"
            android:tint="?attr/colorOnSurfaceVariant"
            android:layout_marginStart="8dp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
